# register-microservice 

## Description
The `register-microservice` is responsible for user registration.

## Features
- - Create new user accounts
- Validate user input
- Store user details securely

## Setup & Usage
1. Install dependencies: `npm install`
2. Start the service: `npm start`
3. API endpoints:
   - `POST /register`
