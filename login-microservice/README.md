# login-microservice 

## Description
The `login-microservice` is responsible for user authentication, verifying credentials, and issuing authentication tokens.

## Features
- - User login authentication
- Token generation and validation
- Secure password storage

## Setup & Usage
1. Install dependencies: `npm install`
2. Start the service: `npm start`
3. API endpoints:
   - `POST /login`
